import React, { useState } from "react";
import HomeComponent from "./components/HomeComponent";

const App: React.FC = () => {
  return (
    <div>
      <HomeComponent />
    </div>
  );
};

export default App;
